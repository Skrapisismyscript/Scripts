repeat wait() until game:IsLoaded()
task.wait(1)
print("check one")

for i,v in pairs(getconnections(game:GetService("Players").LocalPlayer.Idled)) do
v:Disable()
print('done')
end


print("done check now start script")
task.wait(3)
print('started')
if game.PlaceId == 1458767429 then
    
    if #game.Players:GetPlayers() == 1 then
           local windows = loadstring(game:HttpGet("https://raw.githubusercontent.com/ZepsyyCodesLUA/Synapse-Library-OBFUSCATED-/main/Source.lua"))()
local win = windows:Create({
    Title = "Quest Helper ABA SCRIPT",
    Game = "Anime Bugs Arena[ABA]"
})

local tab = win:NewTab({
    Title = "Main"
})

tab:Label({
    Title = "WARNING: DONT CLICK TP IMMEDIATLEY AFTER GETTING EXP, GIVE IT SOME TIME SO THE DATA DOESNT GLITCH. "
})

tab:Button({
    Title = "TP to afk world",
    Callback = function()
       game:GetService("TeleportService"):Teleport(5411459567, game.Players.LocalPlayer)
    end
})
    
local eventbypass

eventbypass = hookmetamethod(game, "__namecall", function(self, ...)
    local method = getnamecallmethod()
    local args = {...}

    if self.Name == "Setup" and method == "FireServer" then
       
    end

    return eventbypass(self, ...)
end)
    else
        print("detect public teleporting private")
        local args = {
            [1] = "join",
            [2] = "4dYc!K3"
        }
        
        game:GetService("ReplicatedStorage").PS:FireServer(unpack(args))
        
        print("remote fired wait for game")
        
        

    end

    
elseif game.PlaceId == 5411459567 then
    task.wait(5)
    game:GetService("TeleportService"):Teleport(1458767429, game.Players.LocalPlayer)
end
